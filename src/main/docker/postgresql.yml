version: '2'
services:
  criteria-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/criteria/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=criteria
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
